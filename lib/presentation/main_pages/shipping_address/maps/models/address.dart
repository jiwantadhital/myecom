class Address{
  String? placeFormattedAddress;
  String? placeName;
  String? placeId;
  double? latitude;
  double? longitute;
  Address({ this.latitude, this.longitute, this.placeFormattedAddress, this.placeId, this.placeName});
}